<h1>Lista de Produtos</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Valor</th>
    <th></th>
  </tr>

<% @produtos.each do |prod| %>
  <tr>
    <td><%= prod.nome %></td>
    <td><%= prod.valor %></td>
    <td><%= link_to 'Detalhes', prod %></td>
  </tr>
<% end %>
</table>

<br />
